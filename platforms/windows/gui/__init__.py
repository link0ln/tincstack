"""tincmgr GUI package (PySide6). main.py is the entry point; the modules here
hold the pieces that are testable offscreen: worker threads, the invite/join
dialogs and the transports editor panel."""
